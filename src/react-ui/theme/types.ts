export type Theme = {
  colors: {
    primary: string;
    secondary: string;
    background: string;
    gray: string;
    text: string;
    textLight: string;
  };
  fonts: {
    body: string;
  };
  spacing: {
    xs: string;
    sm: string;
    md: string;
    lg: string;
    xl: string;
    xxl: string;
  };
  scale: {
    default: string;
  };
};
